import styles from './styles.module.scss';

export function Footer() {
    return (
        <footer className={styles.footer}>
            <h2>Desenvolvido por José Ricardo | caverna labs</h2>
        </footer>
    );
}